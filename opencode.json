{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "servers": {
      "stitch": {
        "type": "local",
        "command": [
          "npx",
          "-y",
          "@_davideast/stitch-mcp",
          "proxy"
        ],
        "environment": {
          "STITCH_API_KEY": "{env:STITCH_API_KEY}"
        }
      }
    }
  }
}
